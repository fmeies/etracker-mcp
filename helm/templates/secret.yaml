{{- if .Values.partnerApiKeys }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "etracker-mcp.name" . }}
  labels:
    {{- include "etracker-mcp.labels" . | nindent 4 }}
type: Opaque
stringData:
  PARTNER_API_KEYS: {{ .Values.partnerApiKeys | quote }}
{{- end }}
